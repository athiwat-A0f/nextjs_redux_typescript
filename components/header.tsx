import React, { ReactElement } from 'react'

interface Props {
    
}

export default function header({}: Props): ReactElement {
    return (
        <div>
            <h1>header</h1>
        </div>
    )
}
